# Leave empty to use same-origin /v1/copilot (works with ngrok).
# Override only if the browser must call a different host.
NEXT_PUBLIC_COPILOT_URL=
# Server-side only: where Next proxies /v1/copilot → (default 127.0.0.1:8000)
COPILOT_UPSTREAM_URL=http://127.0.0.1:8000
